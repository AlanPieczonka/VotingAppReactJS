import React from 'react';

const NotFoundPage = () => (
  <div>
    <h1 className="weight300">You are lost!</h1>
    <h2 className="weight300">You'll probably want to go back to Home page</h2>
  </div>
);

export default NotFoundPage;
